Genz AI - FastAPI starter

Run server:
1. pip install -r server/requirements.txt
2. set AI_API_KEY env var (or copy server/.env.example to server/.env and edit)
3. uvicorn server.app:app --host 0.0.0.0 --port 8000

Client: open client/index.html in browser or host client folder on Vercel.
